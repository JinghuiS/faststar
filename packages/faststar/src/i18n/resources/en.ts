import type { TranslationKeys } from "../types"

export const i18nEnTranslation: TranslationKeys = {
    faststar: {
        list: {
            searchForm: {
                search: "Search",
                reset: "Reset"
            },
            actions: "actions"
        }
    }
}
